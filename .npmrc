; Build/test environment settings. Do not run package lifecycle scripts on
; install (supply-chain hardening). Provenance is enabled explicitly on the
; publish command line in CI, not here.
ignore-scripts=true
